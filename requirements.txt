fastapi==0.115.6
# Pinned explicitly: the `mcp` package below pulls a much newer starlette
# by default which breaks fastapi==0.115.6 (requires starlette<0.42). We
# only use mcp's client module (mcp.client.sse), never its server module,
# so the older starlette pin here is safe.
starlette==0.41.3
uvicorn[standard]==0.34.0
pydantic==2.11.9
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
redis==5.2.1
agno==1.8.4
openai==1.66.0
anthropic==0.111.0
mcp==1.28.0
httpx==0.28.1
python-multipart==0.0.20
python-json-logger==0.1.11
structlog==24.4.0
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
sse-starlette==2.2.1
tenacity==9.0.0
python-dotenv==1.0.1
ulid-py==1.1.0